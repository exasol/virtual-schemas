language: java

matrix:
  include:
    - jdk: openjdk11

addons:
  sonarcloud:
    organization: exasol

before_script:
  - tools/version.sh verify

script:
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar

cache:
  directories:
  - "$HOME/.m2"
